# PHASE 5 — Product UX & Onboarding gap audit

> Дата: 2026-07-24 · Ветка: `feat/m5-ux-onboarding` · Автор: терминальный Claude (Opus, M5-полоса)
> Метод: read-only обход `index.html` + `src/app.js` + провайдер-данные (`api/_lib/connector-capabilities.js`,
> `api/connectors/status.js`, `src/publishing/platform-status.js`). Каждый вывод привязан к `file:line`.
>
> **Gate M5 goal:** новый пользователь проходит идея → сценарий → визуалы → голос → рендер → аналитика → экспорт
> БЕЗ чтения исходного кода.

## Сводка

- **Критериев:** 10
- **exists (закрыто):** 0 полностью — 2 близки (responsive, reduced-motion)
- **partial (частично, нужен слой поверх):** 6 — #1, #2, #3, #5, #6, #7, #10
- **missing (нет):** 2 — #4 (demo), #9 в части primary-flow санитайза (данные-провайдера частично)
- **owner-decision / STOP-AND-REPORT:** 1 — #8 (RU+EN i18n: системы нет, с нуля не строим)

Приоритет реализации (по ценности для «нового пользователя без кода»): **#9 → #2/#10 → #5 → #4 → #7 → #1**.

## Таблица критериев

| # | Критерий | Текущее состояние (file:line) | Гэп | Планируемый срез |
|---|----------|-------------------------------|-----|------------------|
| 1 | First-run wizard: local-only / free / BYOK / cloud | **partial.** Welcome-оверлей есть: `index.html:1265-1279` (`welcomeOverlay`, `role="dialog"`, `aria-modal`, поле темы, «Начать» / «Осмотреть доску»), lead-текст упоминает «локально» и «свой или бесплатный ключ» (`index.html:1269`). Wizard BYOK-пресеты: `src/app.js:88` (`WIZARD_BYOK_PRESETS`), модель-селект `src/app.js:1053-1078`. localStorage-gated. | Оверлей не различает 4 режима (local-only / free / BYOK / optional cloud) как явный выбор; новичок не понимает, что можно вообще без ключа. Нет краткого «как это работает / что нужно». | Срез #1: полировка wizard/overlay — явная строка про режимы (локально бесплатно / бесплатный ключ / свой ключ / облако опц.). |
| 2 | Provider diagnostics: installed / configured / healthy / unavailable / **reason** | **partial (данные есть, не показаны человеку).** Богатый бэкенд: `getConnectorCapabilityStatus` (`api/_lib/connector-capabilities.js:61`) отдаёт per-capability `state`/`executable`/`blockers`/`providers[].configured`, роут `api/product.js:151` (`route=connectors/capabilities`). Ещё `api/connectors/status.js` (configured booleans) и `provider-catalog` `freeMode`. UI показывает только BYOK-слоты (`src/app.js:1272+`, configured/missing) и **dev-дампы** (`formatReadinessReport` `src/app.js:2644`, `formatAgentPlan` `2681` — англ. «Status: unknown», «Blockers:»). | Нет единой человеко-читаемой панели «провайдер → установлен/настроен/готов/недоступен + причина». Причины (`blockers`: `adapter_not_implemented`, `oauth_token_exchange_not_implemented`, `provider_credentials_missing`) не переведены в понятный текст. | Срез #2: панель диагностики провайдеров из capabilities-данных; TDD на маппинг state/blocker → человеко-строка. |
| 3 | Guided workflow idea→script→images/broll→voice→render→analytics→publish/export | **partial.** Есть вход: шапка-CTA «🎬 Тема→видео» + welcome→wizard (`index.html:1268`), панели всех этапов (script `index.html:1087`, publish `1252`, render/analytics `localRenderAnalytics`), авто-тур `playTour` (`index.html:978`). | Нет видимой «следующий шаг» навигации, связывающей этапы; пользователь должен сам угадать порядок панелей. Полный stepper — крупная работа. | Частично закрыто существующим туром + CTA. Полный визуальный stepper → owner-decision (не в этом M5-срезе, чтобы не редизайнить). |
| 4 | Demo project БЕЗ секретов | **missing.** Единственные «demo» — про demo-storage backend (`src/app.js:2633`), не про образец доски. Нет кнопки «Загрузить пример». | Новичок не видит готового примера полной доски (карточки/сценарий/раскадровка) без запуска пайплайна. | Срез #4: «Загрузить демо» — детерминированный sample-board (без секретов, без сети). TDD на форму демо-доски. |
| 5 | Empty / loading / error / retry на каждой async-панели | **partial.** Draft/render: loading (`statusEl`/`localRenderStatus` «…»), error (`draftErrorText` `src/app.js:1201`, `renderErrorText` `2795`), retry (re-enable кнопок), poll-timeout. Провайдеры: catch (`src/app.js:1279`). | Часть async-панелей (public research `formatResearchResults` `2705`, readiness/connector-guide, API-каталог `2131`) без явных loading/empty/retry-состояний; пустой результат обрабатывается текстом, но без единого паттерна. | Срез #5: добить loading/empty/retry там, где аудит показал пробел. |
| 6 | Responsive (mobile 375px) | **partial → близко.** Media-queries `index.html:293` (`max-width:520px`), `889` (`max-width:860px`), welcome-actions wrap на 520 (`index.html:293-296`), много `min-width:0` для flex. | Нет явной проверки на 375px; узкие места (шапка-кнопки, wizard-грид) не верифицированы на 375. | Лёгкая проверка/точечные фиксы переполнений в рамках срезов #2/#7 (без редизайна). |
| 7 | Accessibility: labels, focus, keyboard, contrast, reduced-motion | **partial.** aria: 5×`aria-label`, 4×`aria-live`, `role="dialog"`+`aria-modal` (welcome), `role="status"`×2, `prefers-reduced-motion: reduce` honored (`index.html:126`). | Иконочные кнопки без aria-label (Тур/Перевод/Аккаунты и др.); фокус при открытии welcome-оверлея не подтверждён; клавиатурная операбельность кастомных контролов (карточки, connect-режим) не гарантирована; контраст mini-note не проверен. | Срез #7: aria-labels на иконочные кнопки, focus-trap/фокус на welcome, keyboard, honored reduced-motion (уже есть — закрепить). |
| 8 | RU + EN (только если locale-система уже есть) | **missing system.** `index.html:2` `lang="ru"`, весь UI на русском. i18n/locale-фреймворка нет (grep `i18n\|locale\|translations`). `buildTranslationPack` (`src/app.js:1655`) — перевод **произведённого контента**, не UI. | Двуязычие UI требует i18n-системы с нуля (большая работа). | **STOP-AND-REPORT / owner-decision.** Не строим с нуля в этом срезе. |
| 9 | Нет внутренних терминов / stack traces / raw JSON / dev-дампов в UI | **partial (реальные гэпы).** Primary-flow ошибки показывают сырой `error.message`-код: `draftErrorText` `src/app.js:1206`, `renderErrorText` `2798`, provider-key `1315`, и ~10× `Ошибка: ${error.message \|\| "unknown"}` (`1767,1791,1814,1846,1887,1972,2014,2038`). `error.message` бывает `draft_poll_timeout`, `local_render_poll_timeout`, `http_500`, `catalog_403`, `api_key_required` — dev-коды. `job.error` показывается «Детали: …» (`2891`). Dev-дампы: `formatReadinessReport`/`formatAgentPlan`/`formatStorageStatus` (англ. «Status: unknown», «Gates:», «Blockers:»). | Пользователь видит машинные коды и англоязычные dev-дампы вместо дружелюбного текста. | **Срез #9 (TOP, TDD):** pure `src/ui/user-messages.js` — маппинг код/message → понятная RU-строка; проводка во все primary-flow error-пути; unit-тест маппинга. Dev-дампы (readiness/agent-plan) — за диагностическими кнопками; смягчить/пометить как «для разработчика», не приоритет. |
| 10 | Free vs paid провайдеры визуально различимы | **partial.** Данные есть: `provider-catalog` `freeMode` (`free_tier`/`free_plan`/`no_key`/`paid_or_trial`, `src/app.js:33-40`), wizard-лейблы «(бесплатно)» (`1053-1056`). | Нет единого визуального бейджа free/paid/no-key по всему провайдер-UI. | Покрывается срезом #2 (панель диагностики с бейджами free/paid/no-key). |

## Планируемые срезы (порядок реализации)

1. **Срез #9 — санитайз ошибок (TDD).** `src/ui/user-messages.js` (чистый ES-модуль, как `card-image.js`), unit-тест в `test/unit/`, проводка в `draftErrorText`/`renderErrorText`/provider-key/аккаунт-пути. Убирает `*_poll_timeout`, `http_500`, `catalog_403` и т.п. из UI.
2. **Срез #2+#10 — панель диагностики провайдеров.** Тянет `route=connectors/capabilities`, рендерит человеко-читаемо: установлен/настроен/готов/недоступен + причина, бейдж free/paid/no-key. Unit-тест на маппинг state/blocker/freeMode → лейбл.
3. **Срез #5 — состояния async-панелей.** Добить loading/empty/retry там, где нет (research, каталог).
4. **Срез #4 — demo project.** Кнопка «Загрузить пример» → детерминированная sample-доска (без секретов, без сети). Unit-тест формы.
5. **Срез #7+#1 — a11y + wizard-полировка.** aria-labels, фокус на welcome, keyboard, reduced-motion; явные режимы local/free/BYOK/cloud в оверлее. Заодно проверка 375px (#6).

## Реализовано в этой сессии (feat/m5-ux-onboarding)

| Срез | Критерии | Что сделано | Тесты | Commit |
|------|----------|-------------|-------|--------|
| Санитайз ошибок | #9 | `src/ui/user-messages.js` (коды/http_*/catalog_* → RU), проводка во все primary-flow error-пути; `job.error` через `humanizeDetail` | +8 unit | c15a1e0 |
| Диагностика провайдеров | #2, #10, #5 | Панель «готов/настроен/недоступен + причина» из `connectors/capabilities`, бейдж free/paid/no-key/OAuth, loading/empty/error+retry | `connector-labels.js` +7 unit, +1 UI | 8de7f79 |
| One-click demo | #4 | `src/ui/demo-project.js` (детерминированный, без сети/ключей); входы: welcome-оверлей + wizard-панель | +5 shape, +1 UI | 170a816 |
| a11y + wizard | #7, #1 | aria-label на 11 иконочных кнопок; 4 режима в оверлее (локально/бесплатный ключ/BYOK/облако) | +2 UI | 0d035e8 |

Лёгкий гейт на финале: `validate ok · 552 unit / 0 fail · build exit 0`.
CI-PENDING (не заявлять done): полный `npm run check` (media/publish/workspace/smoke/verify:bundle) — облако.

## STOP-AND-REPORT (владельцу, без реализации в этом срезе)

- **#8 RU+EN i18n** — системы нет; строить с нуля = крупная работа (обёртка `t()`, словари, переключатель, покрытие всего UI). Требует решения владельца об объёме и приоритете.
- **#3 полный визуальный stepper** пайплайна — если вырастет в редизайн навигации; сейчас частично закрыт туром + CTA.
- Любые касания OAuth/publish token exchange и `src/media/*` render-internals — вне scope M5.
